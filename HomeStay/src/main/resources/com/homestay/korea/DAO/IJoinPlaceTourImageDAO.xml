<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.homestay.korea.DAO.IJoinPlaceTourImageDAO">
	
	<select id="readWithContentid" resultType="com.homestay.korea.DTO.JoinPlaceTourImage">
		select * from tour_image where contentid=#{contentid}
	</select>
	

	<select id="readWithThemeLocationStartEndOrderByParm" parameterType="map" resultType="com.homestay.korea.DTO.JoinPlaceTourImage">
		SELECT place.contentid as contentid, createdtime, modifiedtime, theme, count, location, imageurl, imageno 
		FROM place, tour_image 
		WHERE place.contentid = tour_image.contentid 
            AND place.theme = "문화시설"
            AND place.location = "경기"
        Order BY count
        LIMIT 0, 10
	</select>
	

	
</mapper>