<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.homestay.korea.DAO.ITourImageDAO">





	
	<select id="readWithContentid" resultType="com.homestay.korea.DTO.TourImageDTO">
		select * from tour_image where contentid=#{contentid}
	</select>
	
	<select id="readWithThemeLocationLimit" parameterType="map" resultType="com.homestay.korea.DTO.TourImageDTO">
		SELECT * FROM tour_image where theme=#{theme} AND location=#{location} limit #{limit}
	</select>
	
</mapper>